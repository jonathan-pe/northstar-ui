/**
 * Northstar **Toast** (Sonner) — import from `northstar-ui/primitives`.
 * Mount `Toaster` once; call `toast()` for transient messages; catalog id `toast`.
 */
import { Toaster } from '@/components/ui/sonner'
import { toast } from 'sonner'

export { Toaster, toast }
