/**
 * Northstar **Toggle** — import from `northstar-ui/primitives`.
 * Two-state button for toolbars; `toggleVariants` for class reuse; catalog id `toggle`.
 */
import { Toggle, toggleVariants } from '@/components/ui/toggle'

export { Toggle, toggleVariants }
