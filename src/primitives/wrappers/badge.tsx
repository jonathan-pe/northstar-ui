/**
 * Northstar **Badge** — import from `northstar-ui/primitives`.
 * Status and label chips; re-exports `badgeVariants` for class-only styling; catalog id `badge`.
 */
import { Badge, badgeVariants } from '@/components/ui/badge'

export { Badge, badgeVariants }
